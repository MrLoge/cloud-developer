/**
 * Fields in a request to store a reference to a photo item.
 */
export interface StorePhotoRequest {
    photoId: string
    thumbUrl: string
    description: string
    photographerName: string
  }
  